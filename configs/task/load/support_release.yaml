# @package task
family: load
path: configs/task/load/support_release.yaml
definition:
  schema_version: 1
  name: support_release
  # 质量与惯性一起进入物理模型；在默认 g=9.81 m/s² 下自重为 2.5 N。
  cube_mass_kg: 0.254841997961264
  friction_coefficient: 0.8
  object_material: hard
  downward_load:
    interpolation: hold
    waypoints:
      - {t_s: 0.0, force_n: 0.0}
      # 突增来自预载后撤去支撑，不施加额外向下外力。
      - {t_s: 4.0, force_n: 0.0}
  control_period_s: 0.004
