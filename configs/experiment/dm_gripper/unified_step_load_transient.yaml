# @package _global_
defaults:
  - /experiment/dm_gripper/unified_step_load_fast
  - _self_

experiment:
  name: dm_unified_step_load_transient
task:
  definition:
    unified_adaptive:
      load:
        # 缩短承载观测滞后；保留法向力滤波与导纳动力学，只用于仿真验证。
        filter_tau_s: 0.01
