# 正式研究统一入口；research 组同时提供研究元数据和唯一权威矩阵。
defaults:
  - research: force_controller_selection/study
  - execution: study_plan
  - _self_

schema_version: 1

hydra:
  job:
    chdir: false
  run:
    dir: ${execution.output_root}/${study.kind}/${now:%Y%m%dT%H%M%S%f}
