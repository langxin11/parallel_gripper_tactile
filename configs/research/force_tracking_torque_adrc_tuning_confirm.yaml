# @package _global_
# 两阶段调参第二阶段（confirm）默认入口：读取粗扫谱系并做复验。
defaults:
  - study: force_tracking_torque_adrc_tuning_confirm
  - study_execution: plan
  - _self_

schema_version: 1

hydra:
  job:
    chdir: false
  run:
    dir: ${execution.output_root}/${study.kind}/${study.stage}/${now:%Y%m%dT%H%M%S%f}
