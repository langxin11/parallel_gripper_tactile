# @package _global_
# 将 `robotiq_discrete_force.yaml` 声明为默认 plan 场景，后续可通过 study_execution=run 切换执行。
defaults:
  - study: robotiq_discrete_force
  - study_execution: plan
  - _self_

schema_version: 1

hydra:
  job:
    chdir: false
  run:
    dir: ${execution.output_root}/${study.kind}/${now:%Y%m%dT%H%M%S%f}
