# @package _global_
# 将 `dm_admittance_tuning.yaml` 声明为默认 plan 场景，后续可通过 study_execution=run 切换执行。
defaults:
  - study: dm_admittance_tuning
  - study_execution: plan
  - _self_

schema_version: 1

hydra:
  job:
    chdir: false
  run:
    dir: ${execution.output_root}/${study.kind}/${now:%Y%m%dT%H%M%S%f}
